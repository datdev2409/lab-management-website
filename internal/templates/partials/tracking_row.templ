package partials

import "github.com/datdev2409/lab-admin-go/internal/models"

templ TrackingRow(tracking *models.Tracking) {
	<tr>
		<td>{ tracking.Name }</td>
		<td>
			for _, test := range tracking.Tests {
				<span class="badge bg-secondary me-1">{ test.TestName }</span>
			}
		</td>
	</tr>
}
